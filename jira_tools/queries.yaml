#
#   This file should be located in ~/.jira/config.yaml
#

#
# -- Change to enable server dependent queries
#
# -- NOTE: In retrospect, it seems like it was a mistake to key queries on the server...
servers:
    default:
        compare_priorities:
            search:     'project = "{sproject}" AND "Platform/Program" = "{splatform}" ORDER BY "Global ID" ASC'
            # TODO: Check: shouldn't this qualify by project as well?
            match:      '"Platform/Program" = "{tplatform}" AND "Android Version(s)" = "{taversion}" AND "Global ID" ~ {gid}'

        dump_parents:
            search:     'project = AREQ AND "Platform/Program" = "{splatform}" AND issuetype = E-Feature AND priority != P4-Low ORDER BY priority ASC'
            match:      'key = "{key}"'

        e_feature_scanner:
            # -- Find entries to (potentially) clone and modify...
            candidate_e-features:     'project = AREQ AND issuetype = E-Feature AND status in (Open, "In Progress", Closed, Merged, Blocked) AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            parent_query:             'key = "{parent_key}"'
            sibling_query:            'parent="{parent_key}" and "Android Version(s)" in ("{tversion}") AND "Platform/Program" in ("{tplatform}")'

        scan_areq_and_check_for_preq:
            candidate_entries:        'labels in ("IOTG_IVI") AND issuetype = E-Feature and project = AREQ AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            # -- TODO: Does the PREQ target have to be labeled with IOTG_IVI?
            target_entry:           'key = "{key}" AND project = PREQ AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            # target_entry:           'key = "{key}" AND labels in ("IOTG_IVI") AND project = PREQ AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'

        copy_platform_to_platform:
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            # target_feature_query:      'project = AREQ AND issuetype=Feature AND status not in (REJECTED) AND summary~"%s"'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}] %s'

        add_label_to_platform_version:
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\\\[AaaG\\\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) order by key'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\[AaaG\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) and status != Rejected order by key'

        find_added_dng_vs_jira:
            preq_source_query:         'project = "{sproject}" AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) order by key'

    jira-t3:
        E_Feature_by_Parent:
            # The list of Features who might be parents to the E-Features we want to update
            areq_feature_query:         'project = AREQ AND issuetype = Feature AND component = Kernel AND "Platform/Program" in ("Broxton-P IVI", "Broxton-P IVI (AaaG-CWP)", "Icelake-U SDC", "Tigerlake SDC") ORDER BY "Platform/Program", "Android Version(s)", key'
            # The list of possible E-Features who might be children of the Features we've selected to update
            areq_e_feature_query:       '"Android Version(s)" in (O, O-MR1) AND project = AREQ AND issuetype = E-Feature AND component = Kernel AND "Platform/Program" in ("Broxton-P IVI", "Broxton-P IVI (AaaG-CWP)", "Icelake-U SDC", "Tigerlake SDC") ORDER BY "Platform/Program", "Android Version(s)", key'
            # The intersection of the above two groups where the Feature is the parent of the
            # E-Feature, is the group of items we want to update...
            platform/program:           ["Broxton-P IVI", "Broxton-P IVI (AaaG-CWP)", "Broxton-P IVI (AaaG-Xen)", "Icelake-U SDC", "Tigerlake SDC"]
            android_version:            ["O", "O-MR1"]

        compare_priorities:
            search:     'project = "{sproject}" AND "Platform/Program" = "{splatform}" ORDER BY "Global ID" ASC'
            # TODO: Check: shouldn't this qualify by project as well?
            match:      '"Platform/Program" = "{tplatform}" AND "Android Version(s)" = "{taversion}" AND "Global ID" ~ {gid}'

        dump_parents:
            search:     'project = AREQ AND "Platform/Program" = "{splatform}" AND issuetype = E-Feature AND priority != P4-Low ORDER BY priority ASC'
            match:      'key = "{key}"'

        e_feature_scanner:
            # -- Find entries to (potentially) clone and modify...
            # candidate_e-features:     'key=AREQ-22918 and project = AREQ AND issuetype = E-Feature AND status in (Open, "In Progress", Closed, Merged, Blocked) AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            candidate_e-features:     'project = AREQ AND issuetype = E-Feature AND status in (Open, "In Progress", Closed, Merged, Blocked) AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            parent_query:             'key = "{parent_key}"'
            sibling_query:            'parent="{parent_key}" and "Android Version(s)" in ("{tversion}") AND "Platform/Program" in ("{tplatform}")'

        e_feature_scanner_AREQ-22918:
            # -- Find entries to (potentially) clone and modify...
            candidate_e-features:     'key=AREQ-22918 and project = AREQ AND issuetype = E-Feature AND status in (Open, "In Progress", Closed, Merged, Blocked) AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            parent_query:             'key = "{parent_key}"'
            sibling_query:            'parent="{parent_key}" and "Android Version(s)" in ("{tversion}") AND "Platform/Program" in ("{tplatform}")'

        scan_areq_and_check_for_preq:
            candidate_entries:        'labels in ("IOTG_IVI") AND issuetype = E-Feature and project = AREQ AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            # -- TODO: Does the PREQ target have to be labeled with IOTG_IVI?
            target_entry:           'key = "{key}" AND project = PREQ AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            # target_entry:           'key = "{key}" AND labels in ("IOTG_IVI") AND project = PREQ AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'

        copy_platform_to_platform:
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}] %s'

        copy_platform_to_platform_AREQ-25937:
            # Copy all of source platform to target platform
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND priority in (P1-Stopper, P2-High, P3-Medium)'
            preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND priority in (P1-Stopper, P2-High, P3-Medium)'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND priority in (P1-Stopper, P2-High, P3-Medium)'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND priority in (P1-Stopper, P2-High, P3-Medium)'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}] %s'

        "Copy [O][Icelake-U SDC] to [O][Icelake-U SDC][AaaG]":
            # Based on copy_platform_to_platform_ALL:
            # -- We don't want ANY preq's!
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\\\[AaaG\\\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) order by key'
            preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND summary~"\\\\[AaaG\\\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) order by key'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\[AaaG\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) and status != Rejected order by key'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND summary~"\\[AaaG\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) and status != Rejected order by key'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND summary~"\\[AaaG\\]" AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}][AaaG] %s'

        "Copy [O][Broxton-P IVI] to [O-MR1][Broxton-P IVI]":
            # -- AREQ-24757, AREQ-25918
            #preq_source_query:         'key="PREQ-23800" and project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) order by key'
            #areq_source_e_feature:     'key="AREQ-21218" and project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) order by key'
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) order by key'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) order by key'
            # preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) order by key'
            preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) order by key'
            # areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) order by key'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) order by key'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}] %s'

        add_label_to_platform_version:
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\\\[AaaG\\\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) order by key'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\[AaaG\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) and status != Rejected order by key'

        find_added_dng_vs_jira:
            preq_source_query:         'project = "{sproject}" AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) order by key'

    jira01:
        project_to_csv:
            # items_query:     'project={sproject} and "Platform/Program" in ({splatform}) and "Software Version" in ({sversion}) and type in (Epic) order by key'
            items_query:     'project={sproject} and "Exists On" in ({sexists_on}) and "Software Version" in ({sversion}) and type in (Epic) order by "Epic Name"'


        E_Feature_by_Parent:
            # The list of Features who might be parents to the E-Features we want to update
            # areq_feature_query:         'project = AREQ AND issuetype = Feature AND component = Kernel AND "Platform/Program" in ("Broxton-P IVI", "Broxton-P IVI (AaaG-CWP)", "Broxton-P IVI (AaaG-Xen)", "Icelake-U SDC", "Tigerlake SDC") ORDER BY "Platform/Program", "Android Version(s)", key'
            areq_feature_query:         'project = AREQ AND issuetype = Feature AND status = Candidate AND component = Kernel AND labels = PKT AND assignee in (arocchia, currentUser()) order by key'
            # The list of possible E-Features who might be children of the Features we've selected to update
            areq_e_feature_query:       '"Android Version(s)" in (O, O-MR1) AND project = AREQ AND issuetype = E-Feature AND component = Kernel order by key'
            # The intersection of the above two groups where the Feature is the parent of the
            # E-Feature, is the group of items we want to update...
            # platform/program:           ["Broxton-P IVI", "Broxton-P IVI (AaaG-CWP)", "Broxton-P IVI (AaaG-Xen)", "Icelake-U SDC", "Tigerlake SDC"]
            # android_version:            ["O", "O-MR1"]

        compare_priorities:
            search:     'project = "{sproject}" AND "Platform/Program" = "{splatform}" ORDER BY "Global ID" ASC'
            # TODO: Check: shouldn't this qualify by project as well?
            match:      '"Platform/Program" = "{tplatform}" AND "Android Version(s)" = "{taversion}" AND "Global ID" ~ {gid}'

        dump_parents:
            search:     'project = AREQ AND "Platform/Program" = "{splatform}" AND issuetype = E-Feature AND priority != P4-Low ORDER BY priority ASC'
            match:      'key = "{key}"'

        e_feature_scanner:
            # -- Find entries to (potentially) clone and modify...
            candidate_e-features:     'project = AREQ AND issuetype = E-Feature AND status in (Open, "In Progress", Closed, Merged, Blocked) AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            # candidate_e-features:     'Classification=Functional and project=AREQ AND issuetype = E-Feature AND status in (Open, "In Progress", Closed, Merged, Blocked) AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            parent_query:             'key = "{parent_key}"'
            sibling_query:            'parent="{parent_key}" and "Android Version(s)" in ("{tversion}") AND "Platform/Program" in ("{tplatform}")'

        scan_areq_and_check_for_preq:
            candidate_entries:        'labels in ("IOTG_IVI") AND issuetype = E-Feature and project = AREQ AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            # -- TODO: Does the PREQ target have to be labeled with IOTG_IVI?
            target_entry:           'key = "{key}" AND project = PREQ AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            # target_entry:           'key = "{key}" AND labels in ("IOTG_IVI") AND project = PREQ AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'

        copy_platform_to_platform:
            # Qualified copy for AREQ-24774
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}] %s'

        copy_platform_to_platform_Video_Only:
            # -- AREQ-25268: Import "Video" REQ Database from BXT IVI O, O MR1 to KBL-POS
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component in (Video)'
            preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component in (Video)'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component in (Video)'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component in (Video)'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}] %s'

        copy_platform_to_platform_AREQ-25937:
            # Copy all of source platform to target platform
            preq_source_query:         'Project = PREQ AND Classification = "Functional Use Case" AND issuetype = UCIS AND "Platform/Program" = "Broxton-P IVI" AND "Android Version(s)" in (O, O-MR0) AND Status != Rejected AND "Exists On" in ("BXT-P-GP-MRB VM") AND priority = P1-Stopper AND component != Documentation ORDER BY key DESC'
            preq_target_query:         'Project = PREQ AND Classification = "Functional Use Case" AND issuetype = UCIS AND "Platform/Program" = "Broxton-P IVI" AND "Android Version(s)" in (O, O-MR0) AND Status != Rejected AND "Exists On" in ("BXT-P-GP-MRB VM") AND priority = P1-Stopper AND component != Documentation ORDER BY key DESC'
            areq_source_e_feature:     'Project = AREQ AND issuetype = E-Feature AND Classification = Functional AND "Platform/Program" = "Broxton-P IVI" AND "Android Version(s)" in (O, O-MR0) AND Status != Rejected AND "Exists On" in ("BXT-P-GP-MRB VM") AND priority = P1-Stopper AND component != Documentation ORDER BY key DESC'
            areq_target_e_feature:     'Project = AREQ AND issuetype = E-Feature AND Classification = Functional AND "Platform/Program" = "Broxton-P IVI" AND "Android Version(s)" in (O, O-MR0) AND Status != Rejected AND "Exists On" in ("BXT-P-GP-MRB VM") AND priority = P1-Stopper AND component != Documentation ORDER BY key DESC'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}] %s'

        "Copy [O][Icelake-U SDC] to [O][Icelake-U SDC][AaaG]":
            # Based on copy_platform_to_platform_ALL:
            # -- We don't want ANY preq's!
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\\\[AaaG\\\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) order by key'
            preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND summary~"\\\\[AaaG\\\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) order by key'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\[AaaG\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) and status != Rejected order by key'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND summary~"\\[AaaG\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) and status != Rejected order by key'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND summary~"\\[AaaG\\]" AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}][AaaG] %s'

        "Copy [O][Broxton-P IVI] to [O-MR1][Broxton-P IVI]":
            # -- AREQ-24757, AREQ-25918
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) order by key'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) order by key'
            # preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) order by key'
            preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) order by key'
            # areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) order by key'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) order by key'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}] %s'

        "Copy [O][Broxton-P IVI] to [O-MR1][Broxton-P IVI] Rejected and AaaG":
            # -- AREQ-24757, AREQ-25918
            #preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\\\[AaaG\\\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) AND not status in (Open) order by key'
            #areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\[AaaG\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) and status != Rejected AND not status in (Open) order by key'
            preq_source_query:         '(summary ~ "AaaG" or status in (Rejected) or priority in (P4-Low, Undecided)) AND project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) order by key'
            preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) order by key'
            areq_source_e_feature:     '(summary ~ "AaaG" or status in (Rejected) or priority in (P4-Low, Undecided)) AND project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) order by key'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) order by key'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}] %s'

        "AREQ-26896 Clone Broxton-Joule AREQs to KBL-AIA":
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Exists On" = BXT-Joule order by key'
            preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Exists On" = BXT-Joule order by key'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Exists On" = BXT-Joule order by key'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Exists On" = BXT-Joule order by key'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}] %s'

        "AREQ-27297 Clone the XLS list of AREQS to [O][KBL-AIA]":
            # preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Exists On" = BXT-Joule order by key'
            preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Exists On" = BXT-Joule order by key'
            # areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Exists On" = BXT-Joule order by key'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Exists On" = BXT-Joule order by key'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}] %s'

        "OPM-112 Create O-MR1 AaaG-CWP AREQs":
            # -- PREQ not used...
            # preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Exists On" = BXT-Joule order by key'
            # preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Exists On" = BXT-Joule order by key'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND status in (Open, Blocked, "In Progress", Closed, Merged) AND priority in (P1-Stopper, P2-High) AND "Android Version(s)" = {sversion} AND "Platform/Program" in ("{splatform}")'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) order by key'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}] %s'

        add_label_to_platform_version:
            # Note that this EXCLUDES [AaaG], P-4's and Rejected items.
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\\\[AaaG\\\\]" AND priority in (P1-Stopper, P2-High, P3-Medium, P4-Low) order by key'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\[AaaG\\]" AND priority in (P1-Stopper, P2-High, P3-Medium, P4-Low) and status != Rejected order by key'

        add_label_to_platform_version_P4:
            # Note that this EXCLUDES [AaaG], P-4's and Rejected items.
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\\\[AaaG\\\\]" AND priority in (P4-Low) order by key'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\[AaaG\\]" AND priority in (P4-Low) and status != Rejected order by key'

        add_label_to_platform_version_AaaG:
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary~"\\\\[AaaG\\\\]" AND priority in (P1-Stopper, P2-High, P3-Medium, P4-Low) order by key'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary~"\\[AaaG\\]" AND priority in (P1-Stopper, P2-High, P3-Medium, P4-Low) and status != Rejected order by key'


        find_added_dng_vs_jira:
            preq_source_query:         'project = "{sproject}" AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) order by key'


        areq_superceded_by_preq:
            # -- NO QUERIES ARE REQUIRED...

        label_from_labeled_items:
            items_with_label_query:     'project={sproject} and "Platform/Program" in ({splatform}) and "Android Version(s)" in ({sversion}) and type in (E-Feature) and labels in ({slabel}) and status not in (Rejected)'
            targets_for_label_query:    'project={tproject} and "Platform/Program" in ({tplatform}) and "Android Version(s)" in ({tversion}) and type in (E-Feature) and status not in (Rejected)'

    jira-pfh:
        compare_priorities:
            search:     'project = "{sproject}" AND "Platform/Program" = "{splatform}" ORDER BY "Global ID" ASC'
            # TODO: Check: shouldn't this qualify by project as well?
            match:      '"Platform/Program" = "{tplatform}" AND "Android Version(s)" = "{taversion}" AND "Global ID" ~ {gid}'

        dump_parents:
            search:     'project = AREQ AND "Platform/Program" = "{splatform}" AND issuetype = E-Feature AND priority != P4-Low ORDER BY priority ASC'
            match:      'key = "{key}"'

        e_feature_scanner:
            # -- Find entries to (potentially) clone and modify...
            candidate_e-features:     'project = AREQ AND issuetype = E-Feature AND status in (Open, "In Progress", Closed, Merged, Blocked) AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            # candidate_e-features:     'Classification=Functional and project=AREQ AND issuetype = E-Feature AND status in (Open, "In Progress", Closed, Merged, Blocked) AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            parent_query:             'key = "{parent_key}"'
            sibling_query:            'parent="{parent_key}" and "Android Version(s)" in ("{tversion}") AND "Platform/Program" in ("{tplatform}")'

        scan_areq_and_check_for_preq:
            candidate_entries:        'labels in ("IOTG_IVI") AND issuetype = E-Feature and project = AREQ AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            # -- TODO: Does the PREQ target have to be labeled with IOTG_IVI?
            target_entry:           'key = "{key}" AND project = PREQ AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            # target_entry:           'key = "{key}" AND labels in ("IOTG_IVI") AND project = PREQ AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'

        copy_platform_to_platform:
            # Qualified copy for AREQ-24774
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}] %s'

        copy_platform_to_platform_Video_Only:
            # -- AREQ-25268: Import "Video" REQ Database from BXT IVI O, O MR1 to KBL-POS
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component in (Video)'
            preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component in (Video)'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component in (Video)'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component in (Video)'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}] %s'

        copy_platform_to_platform_AREQ-25937:
            # Copy all of source platform to target platform
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND priority in (P1-Stopper, P2-High, P3-Medium)'
            preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND priority in (P1-Stopper, P2-High, P3-Medium)'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND priority in (P1-Stopper, P2-High, P3-Medium)'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND priority in (P1-Stopper, P2-High, P3-Medium)'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}] %s'

        "Copy [O][Icelake-U SDC] to [O][Icelake-U SDC][AaaG]":
            # Based on copy_platform_to_platform_ALL:
            # -- We don't want ANY preq's!
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\\\[AaaG\\\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) order by key'
            preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND summary~"\\\\[AaaG\\\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) order by key'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\[AaaG\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) and status != Rejected order by key'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND summary~"\\[AaaG\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) and status != Rejected order by key'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND summary~"\\[AaaG\\]" AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}][AaaG] %s'

        "Copy [O][Broxton-P IVI] to [O-MR1][Broxton-P IVI]":
            # -- AREQ-24757, AREQ-25918
            # -- We don't want ANY preq's!
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\\\[AaaG\\\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) order by key'
            preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND summary~"\\\\[AaaG\\\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) order by key'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\[AaaG\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) and status != Rejected order by key'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND summary~"\\[AaaG\\]" AND priority in (P1-Stopper, P2-High, P3-Medium) and status != Rejected order by key'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND summary~"\\[AaaG\\]" AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}] %s'

        add_label_to_platform_version:
            # Note that this EXCLUDES [AaaG], P-4's and Rejected items.
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\\\[AaaG\\\\]" AND priority in (P1-Stopper, P2-High, P3-Medium, P4-Low) order by key'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\[AaaG\\]" AND priority in (P1-Stopper, P2-High, P3-Medium, P4-Low) and status != Rejected order by key'

        add_label_to_platform_version_P4:
            # Note that this EXCLUDES [AaaG], P-4's and Rejected items.
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\\\[AaaG\\\\]" AND priority in (P4-Low) order by key'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary!~"\\[AaaG\\]" AND priority in (P4-Low) and status != Rejected order by key'

        add_label_to_platform_version_AaaG:
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary~"\\\\[AaaG\\\\]" AND priority in (P1-Stopper, P2-High, P3-Medium, P4-Low) order by key'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND summary~"\\[AaaG\\]" AND priority in (P1-Stopper, P2-High, P3-Medium, P4-Low) and status != Rejected order by key'


        find_added_dng_vs_jira:
            preq_source_query:         'project = "{sproject}" AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) order by key'

    jira-stg:
        compare_priorities:
            search:     'project = "{sproject}" AND "Platform/Program" = "{splatform}" ORDER BY "Global ID" ASC'
            # TODO: Check: shouldn't this qualify by project as well?
            match:      '"Platform/Program" = "{tplatform}" AND "Android Version(s)" = "{taversion}" AND "Global ID" ~ {gid}'

        dump_parents:
            search:     'project = AREQ AND "Platform/Program" = "{splatform}" AND issuetype = E-Feature AND priority != P4-Low ORDER BY priority ASC'
            match:      'key = "{key}"'

        e_feature_scanner:
            # -- Find entries to (potentially) clone and modify...
            candidate_e-features:     'project = AREQ AND issuetype = E-Feature AND status in (Open, "In Progress", Closed, Merged, Blocked) AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            parent_query:             'key = "{parent_key}"'
            sibling_query:            'parent="{parent_key}" and "Android Version(s)" in ("{tversion}") AND "Platform/Program" in ("{tplatform}")'

        scan_areq_and_check_for_preq:
            candidate_entries:        'labels in ("IOTG_IVI") AND issuetype = E-Feature and project = AREQ AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            # -- TODO: Does the PREQ target have to be labeled with IOTG_IVI?
            target_entry:           'key = "{key}" AND project = PREQ AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'
            # target_entry:           'key = "{key}" AND labels in ("IOTG_IVI") AND project = PREQ AND "Android Version(s)" in ("{sversion}") AND "Platform/Program" in ("{splatform}")'

        copy_platform_to_platform:
            preq_source_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            preq_target_query:         'project = PREQ AND issuetype in standardIssueTypes() AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            areq_source_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            areq_target_e_feature:     'project = AREQ AND issuetype = E-Feature AND "Platform/Program" = "{tplatform}" AND "Android Version(s)" in ({tversion}) AND priority in (P1-Stopper, P2-High, P3-Medium) AND component not in ("Audio Comms",Browser,Camera,Connectivity-Bluetooth,"Connectivity-FM Radio",Connectivity-GNSS,Connectivity-Ethernet,Connectivity-NFC,Documentation,"Energy Management",GPA,Hardware,HDMI,Houdini,"Manufacturing OS",Media,"OTA Update",PnP,"Power Management",Screencast,Sensors,SOCWatch,Telephony,Thermal,Validation,"Vehicle I/O",Video,VMM,"VTune and SEP","Widevine/DRM")'
            # target_feature_query:      'project = AREQ AND issuetype=Feature AND status not in (REJECTED) AND summary~"%s"'
            target_feature_query:      'project = AREQ AND issuetype=Feature AND summary~"%s"'
            target_summary_format:     '[{tversion}][{tplatform}] %s'

        find_added_dng_vs_jira:
            preq_source_query:         'project = "{sproject}" AND issuetype in standardIssueTypes() AND "Platform/Program" = "{splatform}" AND "Android Version(s)" in ({sversion}) order by key'

